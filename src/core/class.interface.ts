/** Every application can have multiple interfaces to interact with its scope
 * Actions should be queueable and manipulate the scope without breaking rules of atomic actions. -- do we need a double interface layer?
*/
export class Interface { }
